# florence_experiments

## Scripts

* timestep_captions.py gets the captions and/or phrase grounding from specific timesteps in the video and stores them in /detailed_captions
* florence_pytorch/embeddings.py  gets the image embeddings cropped with bounding boxes and stores them in /embeddings
* florence_pytorch/main.py text embeddings